apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: fmu-to-resqml
spec:
  environments:
    - name: dev
      build:
        from: main
  components:
    - name: fmu-to-resqml-app
      src: "."
      publicPort: http
      ports:
        - name: http
          port: 5000
